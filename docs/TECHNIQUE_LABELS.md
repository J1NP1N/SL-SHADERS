# ReShade Technique Labels

Required prefixes for experimental SSR work:

- `CORE — ...`
- `SPATIAL — ...`
- `TEMPORAL PRE — ...`
- `TEMPORAL POST — ...`
- `HIZ DEBUG — ...`
- `AVATAR RECEIVER — ...`

Every worker must document exact technique order and isolated-test checkbox state. Avoid ambiguous labels such as `v0.49 Backbone` without a subsystem role.
