CORE+SPATIAL v0.3 -> v0.4 compile-only delta

At SSRFullResMappingPattern():
- float r = fmod(p.x, 7.0) < 1.0 ? 1.0 : 0.0;
- float g = fmod(p.y, 11.0) < 1.0 ? 1.0 : 0.0;
- float b = fmod(p.x + p.y * 3.0, 13.0) < 1.0 ? 1.0 : 0.0;
+ float r = (p.x - floor(p.x / 7.0) * 7.0) < 1.0 ? 1.0 : 0.0;
+ float g = (p.y - floor(p.y / 11.0) * 11.0) < 1.0 ? 1.0 : 0.0;
+ float patternIndex = p.x + p.y * 3.0;
+ float b = (patternIndex - floor(patternIndex / 13.0) * 13.0) < 1.0 ? 1.0 : 0.0;

Technique/file labels advance v0.3 -> v0.4. No production SSR behavior changes.
